name: WarmLambda
description: 
runtime: nodejs
template:
  description: A minimal AWS TypeScript program
  config:
    aws:region:
      description: The AWS region to deploy into
      default: us-east-1
